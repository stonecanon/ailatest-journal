# Keep raw source data (list/) committed so build is reproducible.
# Exclude editor/system junk only.

.DS_Store
*.pyc
__pycache__/
.venv/
venv/
node_modules/
.idea/
.vscode/
*.swp

# Google Drive shortcut placeholders
*.gsheet
*.gdoc
*.gslides


# Build artifacts / intermediate parsed data
generated/

# OpenAlex raw cache (21MB, regeneratable via scripts/fetch_openalex.py)
data/openalex_cache.json


CODEX_REVIEW.md
.wrangler/

# cnkx 旧数据备份
data/cnkx_records.json.bak

# Pre-merge backups
data/journals.json.bak_*

# Track only .gz version for CF Pages 25MB limit; keep .json locally
!data/journals.json.gz
data/journals.json
